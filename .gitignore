# OpenClaw workspace — sensitive files
.env
.env.*
.openclaw/
config-*.json5
config-*.json
secrets/
*.key
*.pem
memory/

# Ignore node_modules if any local dev
node_modules/

# OS files
.DS_Store
# nyanbook secrets (env-based)
.env.nyanbook
*.secrets.json

# GitHub personal access token (never commit)
.github-token
GITHUB_TOKEN

# Workspace runtime files
.nyan-sync-last
vendor/
